[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff]
target-version = "py311"
line-length = 100
exclude = [".agents", "data", "charts"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
